// G - 求平均成绩

/*
# include <stdio.h>
# include <stdlib.h>

int main()
{
	int n, m;
	int* lesson;
	int* stu;
	int cnt = 0;
	int i, j;

	while (scanf("%d%d", &n, &m) != EOF)
	{
		stu = (int*)malloc(sizeof(int) * n);
		lesson = (int*)malloc(sizeof(int) * m);

		for (i = 0; i < n; i++)
		{
			scanf()
		}

		free(lesson);
		free(stu);
	}

	return 0;
}
*/