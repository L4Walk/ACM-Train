// Q - Let the Balloon Rise

/*
# include <stdio.h>
# include <stdlib.h>
# include <string.h>

int main()
{
	int n;
	int cnt = 0;
	int max = 0;
	char str[20];
	char* s;
	int* a;
	int i, j;

	while (scanf("%d", &n) != EOF && n != 0)
	{
		s = (char*)malloc(sizeof(char) * n);
		a = (int*)malloc(sizeof(int) * n);

		while (n > 0)
		{
			scanf("%s", &str);

			for (i = 0; i <= cnt; i++)
			{
				if (str == s[i])

			}


			n--;
		}
	}



	return 0;
}
*/