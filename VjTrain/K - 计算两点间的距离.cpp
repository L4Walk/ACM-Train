// K - 计算两点间的距离

/*
# include <stdio.h>
# include <math.h>

int main()
{
	double x1, x2, y1, y2;
	double length;

	while (scanf("%lf%lf%lf%lf", &x1, &y1, &x2, &y2) != EOF)
	{
		length = sqrt((y2 - y1) * (y2 - y1) + (x2 - x1) * (x2 - x1));

		printf("%.2lf\n", length);
	}

	return 0;
}
*/