// I 世界上最幸福的女孩

/*
#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
	int n, m;
	int* w;
	int* v;
	int i, j;
	int max_value = 0;
	int weight = 0;
	int sum;
	int t = 0;

	scanf("%d%d", &n, &m);

	w = (int*)malloc(sizeof(int) * w);
	v = (int*)malloc(sizeof(int) * v);

	scanf("%d%d", &n, &m);

	for (i = 0; i < n; i++)
	{
		scanf("%d%d", &w[i], &v[i]);
	}

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
		{

		}
	}


	printf("%d", min);

	free(v);
	free(w);

	return 0;
}
*/