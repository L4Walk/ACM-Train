//F	osu£¡

/*
# include <stdio.h>
# include <string>
# include <string.h>

int main()
{
	int i, j, t;
	int len;
	int cnt;
	char str[110];

	while (scanf("%d", &len) != EOF)
	{
		scanf("%s", &str);
		cnt = 0;

		for (i = 0; i < len; i++)
		{
			if (str[i] == 'o')
			{
				for (j = i + 1; j < len; j++)
				{
					if (str[j] == 's')
					{
						for (t = j + 1; t < len; t++)
						{
							if (str[t] == 'u')
								cnt++;
						}
					}
				}
			}
		}

		printf("%d\n", cnt);
	}

	return 0;

	*/