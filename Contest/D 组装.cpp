// D ×é×°

/*
# include <stdio.h>

int main()
{
	long n;
	long long last_size;
	long cnt;
	long i;
	long long temp;
	while (scanf("%ld", &n) != EOF)
	{
		cnt = 0;
		for (i = 0; i < n; i++)
		{
			scanf("%lld", &temp);
			
			if (i == 0 || temp >= last_size)
				cnt++;

			last_size = temp;
		}

		printf("%ld\n", cnt);
	}

	return 0;
}
*/