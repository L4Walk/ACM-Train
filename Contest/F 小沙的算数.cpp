// F Ð¡É³µÄËãÊý

/*
# include <cstdio>
# include <cmath>
# include <cstring>
# include <iostream>
# include <algorithm>
# include <vector>
# include <set>
using namespace std;

# define rep(i,a,n) for(int i=a;i<n;i++)
# define repit(i,a,n) for(auto i = a; i != n;i++)
vector<char>pos;
vector<int>num;

int main()
{
	int n, q;
	int t;
	long sum;
	char ch;
	string s;

	scanf("%d%d", &n, &q);
	cin >> s;

	rep(i, 0, s.length())
	{
		if (s[i] == '*')
			pos.push_back(i);
	}

	rep(i, 0, n)
	{
		scanf("%d", &t);
		num.push_back(t);
	}



	return 0;
}
*/