// 3312: The Number Position

/*
# include <stdio.h>
# include <string.h>

int main()
{
	int n;
	int a[1100];
	int pos;

	while (scanf("%d", &n) != EOF && n != -1)
	{
		memset(a, 0, sizeof(a));
		for (int i = 0; i < n; i++)
		{
			scanf("%d", &a[i]);
		}

		while (scanf("%d", &pos) != EOF && pos != 0)
		{
			printf("%d\n", a[pos - 1]);
		}
	}

	return 0;
}
*/