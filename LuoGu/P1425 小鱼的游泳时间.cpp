// P1425 小鱼的游泳时间
/*

# include <stdio.h>

//int main()
{
    int hour1, hour2, min1, min2;

    scanf_s("%d%d%d%d", &hour1, &min1, &hour2, &min2);

    // totalHour = hour2 - hour1 -1 + (min2 + 60 - min1) / 60 ;
    // totalMin = (min2 + 60 - min1) % 60;

    printf("%d %d", hour2 - hour1 - 1 + (min2 + 60 - min1) / 60, (min2 + 60 - min1) % 60);

    return 0;
}
*/
