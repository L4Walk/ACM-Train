// P5708 【深基2.习2】三角形面积
/*

# include <stdio.h>
# include <math.h>

//int main()
{
    float x, y, z, p;

    scanf_s("%f%f%f", &x, &y, &z);

    p = (x + y + z) / 2;

    printf("%.1f", pow(p * (p - x) * (p - y) * (p - z), 1.0 / 2) );

    return 0;
}
*/
