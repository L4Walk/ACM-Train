// P5715 【深基3.例8】三位数排序


/*
# include <stdio.h>

void swap(int* pa, int* pb)
{
	int t = *pa;
	*pa = *pb;
	*pb = t;
}

int main()
{
	int a, b, c;
	int i;

	scanf_s("%d%d%d", &a, &b, &c);

	if (a > b)
		swap(&a, &b);
	if (a > c)
		swap(&a, &c);
	if (b > c)
		swap(&b, &c);

	printf("%d %d %d", a, b, c);

	return 0;
 }
 */