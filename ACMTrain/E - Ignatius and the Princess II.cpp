// E - Ignatius and the Princess II

/*
# include <stdio.h>
# include <iostream>
# include <cstdio>
# include <vector>
# include <algorithm>
using namespace std;

int main()
{
	int a[10000], count,n, m, i;

	while (cin >> n >> m)
	{
		for (i = 0; i < n; i++)
		{
			a[i] = i + 1;
		}

		count = 1;
		while (next_permutation(a,a+n))
		{
			count++;
			if (count == m)
				break;
		}

		printf("%d", a[0]);
		for (i = 1; i < n; i++)
			printf(" %d",a[i]);
		printf("\n");
	}
		
	return 0;
}*/